#include <stdio.h>
#include <string.h>

int main() {

    char string1[] = "Jack";
    char string2[] = "Kim";

    //strlwr(string1);                              // converts a string to lowercase
    //strupr(string1);                              // converts a string to uppercase
    //strcat(string1, string2);                     // appends string 2 to the end of string 1
    //strncat(string1, string2, 1);                 // appends n characters from string 2 to string 1
    //strcpy(string1, string2);                     // copy string2 to string1
    //strncpy(string1, string2, 1);                 // copy n characters of string2 to string1

    //strset(string1, '?');                         // sets all characters of a string to a given character
    //strnset(string1, 'x', 1);                     // sets first n characters of a string to a given character
    //strrev(string1);                              // reverses a string        

    //int result = strlen(string1);                 // returns the string's length as int
    //int result = strcmp(string1, string2);        // string compare all characters... if they are all same, it will return 0. If different, it returns some other numbers
    //int result = strncmp(string1, string2, 1);    // string compare n characters
    //int result = strcmpi(string1, string1);       // string compare all (ignore case)
    //int reuslt = strncmpi(string1, string1, 1);   // string compare n characters (ignore case)
    
    //printf("%s", string1);
    //printf("%d", result);

    return 0;
}