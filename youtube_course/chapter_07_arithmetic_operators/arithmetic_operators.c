#include <stdio.h>

int main() {

    int x = 5;
    int y = 2;

    //int z = x - y;
    float z = x / (float) y;

    printf("%.2f", z);

    // augmented assignment operators
    // += -= *= /=  etc... pretty much as same as in JAVA

    return 0;
}